# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: uv
description: Python package and project manager
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([uv])"]

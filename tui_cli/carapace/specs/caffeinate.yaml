# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: caffeinate
description: Prevent the system from sleeping
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([caffeinate])"]

# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: jq
description: Command-line JSON processor
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([jq])"]

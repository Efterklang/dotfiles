# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: zellij
description: Terminal workspace with batteries included
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([zellij])"]

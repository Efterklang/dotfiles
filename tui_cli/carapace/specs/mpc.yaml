# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: mpc
description: Music Player Daemon client
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([mpc])"]

# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: bun
description: A fast JavaScript runtime and package manager
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([bun])"]

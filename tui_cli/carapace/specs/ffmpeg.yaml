# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ffmpeg
description: Convert audio and video
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([ffmpeg])"]

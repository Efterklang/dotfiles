# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: rclone
description: Rsync for cloud storage
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([rclone])"]

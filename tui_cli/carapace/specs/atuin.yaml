# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: atuin
description: Magical shell history
parsing: disabled
completion:
  positionalany: ["$carapace.bridge.Fish([atuin])"]
